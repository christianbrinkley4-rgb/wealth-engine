describe("useWizardStep", () => {
  it.todo("defaults to step 1 for invalid query params");
  it.todo("pushes history entries for next and previous navigation");
  it.todo("reports accurate progressPercent and boundary booleans");
});
