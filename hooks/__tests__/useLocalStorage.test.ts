describe("useLocalStorage", () => {
  it.todo("hydrates from localStorage when JSON is valid");
  it.todo("falls back to initial value when storage is unavailable");
  it.todo("clears both React state and localStorage key");
});
